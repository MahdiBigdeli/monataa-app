# راهنمای تبدیل موناتا به اپلیکیشن اندروید

این پوشه یک «PWA» کامل و آماده است (index.html + manifest.json + sw.js + آیکون‌ها).
چون من به اینترنت و ابزار ساخت اندروید (Android SDK) دسترسی مستقیم ندارم، نمی‌تونم خودم فایل APK را کامپایل کنم؛
اما این پوشه دقیقاً همه چیزی است که برای گرفتن APK واقعی لازم داری — فقط دو مرحلهٔ ساده باقی مانده.

## مرحله ۱: آپلود (هاست رایگان، چند ثانیه‌ای)

یکی از این گزینه‌ها را انتخاب کن (هر دو رایگان و بدون نیاز به کدنویسی):

**گزینه الف — Netlify Drop**
۱. برو به app.netlify.com/drop
۲. کل همین پوشه (یا محتویاتش) را با ماوس بکش و رها کن
۳. یک لینک https مثل `https://xxxx.netlify.app` می‌گیری

**گزینه ب — GitHub Pages**
۱. یک ریپازیتوری جدید بساز و فایل‌های این پوشه را آپلود کن
۲. از تنظیمات ریپو، GitHub Pages را روشن کن
۳. لینک `https://username.github.io/repo-name` را خواهی گرفت

## مرحله ۲: ساخت APK با PWABuilder (رایگان، بدون کدنویسی)

۱. برو به pwabuilder.com
۲. لینک https مرحلهٔ قبل را وارد کن و Start را بزن
۳. PWABuilder به‌صورت خودکار manifest و service worker را شناسایی می‌کند (امتیاز باید نزدیک به کامل باشد)
۴. روی "Package for stores" بزن و Android را انتخاب کن
۵. یک فایل APK (یا AAB برای انتشار در Google Play) دانلود می‌شود — همین را روی گوشی نصب کن یا در Play Console آپلود کن

## راه سریع‌تر (بدون APK، فقط برای استفادهٔ شخصی)

اگر فقط می‌خواهی خودت روی گوشی‌ات از اپ استفاده کنی و نیازی به فایل APK نیست:
۱. لینک هاست‌شده (مرحلهٔ ۱) را در مرورگر Chrome گوشی اندرویدت باز کن
۲. از منوی سه‌نقطه، گزینهٔ «افزودن به صفحهٔ اصلی / Install app» را بزن
۳. آیکون اپ روی صفحهٔ اصلی گوشی می‌آید و کاملاً تمام‌صفحه و بدون نوار مرورگر اجرا می‌شود — دقیقاً مثل یک اپ نصب‌شده، حتی بدون اینترنت هم باز می‌شود

## نکته دربارهٔ ذخیرهٔ اطلاعات

اطلاعات (حقوق، هزینه‌ها، دارایی‌ها) در حافظهٔ داخلی همان مرورگر/اپ ذخیره می‌شود. اگر بعداً بخواهی نسخهٔ Play Store منتشر کنی و امکاناتی مثل پشتیبان‌گیری ابری یا ورود با حساب کاربری اضافه کنی، باید یک بک‌اند واقعی (مثل Firebase) به اپ اضافه شود — در آن مرحله هم می‌توانم کمکت کنم.
